import styled from 'styled-components';

import NormalImg from 'components/Img';

const Logo = styled(NormalImg)`
  height: 48px;
  width: 48px;
  margin: 0;
  display: block;
`;

export default Logo;
